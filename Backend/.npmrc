# Backend-specific npm configuration
# This file is specific to the Backend directory
# For global npm settings, see root .npmrc

# Platform configuration
optional=false

# Use npm cache
cache-min=86400

# Don't install dev dependencies in production
production=false

# Platform-specific package resolution
engine-strict=true

# Exclude problematic packages
fund=false

audit=false

# Disable peer dependency warnings
legacy-peer-deps=false

audit=false

