# Enable legacy peer deps to handle React/React Native version conflicts
legacy-peer-deps=true

# Disable engine checks to prevent version conflicts
engine-strict=false

# Force specific React Native version
@react-native/gradle-plugin=0.76.5

# Configure npm to use the latest features
auto-install-peers=true
prefer-offline=true
