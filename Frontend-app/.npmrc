# Frontend-app .npmrc - Expo SDK 54 configuration
# Enable legacy peer deps for React Native compatibility
legacy-peer-deps=true

# Disable engine checks to prevent version conflicts
engine-strict=false

# Configure npm to use the latest features
prefer-offline=true

# Disable funding messages
fund=false
