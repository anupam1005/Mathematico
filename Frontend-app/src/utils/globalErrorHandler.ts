/**
 * Global Error Handler - DISABLED
 * Error handling is done at the source in axios interceptors
 */

export const setupGlobalErrorHandler = () => {
  // No-op - error handling is done in axios interceptors
};
